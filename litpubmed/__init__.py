"""LitPubMed: PubMed search via NCBI E-utilities, fetch via edirect efetch, local SQLite library.

综合问答默认调用阿里云百炼 OpenAI 兼容接口，模型 id 默认为 ``qwen-max``（见 ``litpubmed.config.Settings.llm_model``）。
"""

__version__ = "0.1.0"
